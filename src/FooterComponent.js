import React from "react";
import { Card } from "react-bootstrap";

const FooterComponent = () => {
  return (
    <Card>
     <Card.Footer className="text-muted text-center bg-dark pt-10 mb-0">@CODEVERSE LIMITED</Card.Footer>
    </Card>
  );
};

export default FooterComponent;
